namespace FiveDSudoku.Tools.ScenarioRunner;

/// <summary>
/// Text-based developer diagnostics entry point.
///
/// Phase 0 only proves the project builds and runs; the state summary and the
/// timeline-tree view described in PHASES.md Phase 7 are deliberately not
/// implemented yet.
/// </summary>
internal static class Program
{
    private static int Main(string[] args)
    {
        Console.WriteLine("5D Sudoku ScenarioRunner — skeleton (Phase 0).");
        Console.WriteLine("No scenarios available yet: the core rules engine has not been built.");
        return 0;
    }
}
